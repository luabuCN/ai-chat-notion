# Collab Server 使用根目录的 .env.local 配置
# 在根目录 .env.local 中添加以下变量：

# Database URL (required) - 已有
# POSTGRES_URL=postgresql://user:password@localhost:5432/database

# Authentication Secret (required) - 已有
# AUTH_SECRET=your-auth-secret-here

# Collab Server Port (optional, default: 1234)
COLLAB_SERVER_PORT=1234

# 注意：通过 dotenv-cli 从 ../../.env.local 读取配置

